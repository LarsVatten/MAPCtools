% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-ExploratoryAnalysis.R
\name{plot_counts_edu_race}
\alias{plot_counts_edu_race}
\title{Plot observation counts by education and race across binned time intervals}
\usage{
plot_counts_edu_race(
  df,
  time_var,
  education_var,
  race_var,
  bin_size = NULL,
  bins = NULL,
  labels = NULL,
  plot_title = NULL,
  x_lab = NULL,
  y_lab = "Count"
)
}
\arguments{
\item{df}{A data frame containing at least the columns named by \code{time_var},
\code{education_var}, and \code{race_var}.}

\item{time_var}{String: name of the numeric variable to bin (e.g. \code{"age"},
\code{"year"}, \code{"birth_year"}).}

\item{education_var}{String: name of the education factor variable.}

\item{race_var}{String: name of the race factor variable.}

\item{bin_size}{Single numeric: width of equal-sized bins. Ignored if
\code{bins} is provided.}

\item{bins}{Optional numeric vector of breakpoints to use instead of
\code{bin_size}. Must be length less than or equal to 2.}

\item{labels}{Optional character vector of length \code{length(bins) - 1} for
custom bin labels.}

\item{plot_title}{Title for the plot. Defaults to
\code{"Counts by <time_var> bins, education and race"}.}

\item{x_lab}{Label for the x-axis. Defaults to \code{"<time_var> bin"}.}

\item{y_lab}{Label for the y-axis. Defaults to \code{"Count"}.}
}
\value{
A ggplot2 bar-chart object. Invisibly, the underlying counts
data frame is attached as \verb{attr(<plot>,"data")}.
}
\description{
Takes a data frame, one numeric "time" variable (e.g. age, year, birth_year),
and two discrete grouping variables (education and race). Internally it:
\enumerate{
\item Constructs bins (either equal-width via \code{bin_size}, or from your
explicit \code{bins} vector);
\item Labels them (auto or via your \code{labels});
\item Counts rows in each time_bin x education x race;
\item Produces a dodged bar chart faceted by race.
}
}
